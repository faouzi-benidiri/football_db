meta {
  name: getAllTeams
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/v1/teams?page=0&size=10&sort=name,asc
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 10
  sort: name,asc
  ~sort: acronym,asc
  ~sort: budget,asc
  ~sort: budget,desc
}

script:post-response {
  const response = res.getBody();
  const firstTeam = response.content[0];
  bru.setVar('teamId', firstTeam.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
